import React, { useState } from 'react';

const MyItems = () => {
    const [items, setItems] = useState([]);
    return (
        <div>
            <h2>My Items: </h2>
        </div>
    );
};

export default MyItems;